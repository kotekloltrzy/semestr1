# Utwórz program, który prosi użytkownika o wprowadzenie hasła i sprawdza, czy hasło spełnia określone kryteria
# (np. musi zawierać co najmniej jedną cyfrę i jedną dużą literę)

d = input(f'Podaj hasło które ma co najmniej:\n8 znaków\njedną dużą literkę\njeden znak specjalny\njedną cyfrę\nhasło: ')

if len(d) < 8:
    print("Hasło musi mieć conajmniej 8 znaków")
else:
    ile_duzych = 0
    ile_specjalnych = 0
    for i in d:
        if 'A' <= i <= 'Z':
            ile_duzych += 1
    if ile_duzych < 1:
        print("Hasło musi mieć conajmniej jedną dużą literkę")
    else:
        for a in d:
            if '!' <= a <= '/':
                ile_specjalnych += 1
        if ile_specjalnych < 1:
            print("Hasło musi zawierać coznajmniej jeden znak specjalny")
    print("Nowe hasło spełnia wszystkie kryteria")
